# Copy to `.env` and fill in values. See README for setup.

# Admin timetable / Classes — today: set Calendar timezone in Django admin
# (System settings) and optionally per-user Admin calendar timezone on each User.
# Django TIME_ZONE (below in settings.py) is used only if System Settings is missing/invalid.
